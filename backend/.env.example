PORT=3001
OPENAI_API_KEY=

# Database Configuration - Use either DATABASE_URL (for Neon) or individual parameters
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require
# Alternative: Individual database parameters (if not using DATABASE_URL)
POSTGRES_USER=postgres
POSTGRES_HOST=localhost
POSTGRES_DB=spur_chat
POSTGRES_PASSWORD=
POSTGRES_PORT=5432

# Redis Configuration - Use either REDIS_URL, Upstash REST API, or individual parameters
# Option 1: Redis URL (for services like Railway, Heroku)
REDIS_URL=redis://username:password@host:port

# Option 2: Upstash Redis REST API (recommended for serverless)
UPSTASH_REDIS_REST_URL=https://relieved-dolphin-42063.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here

# Option 3: Traditional Redis parameters (if not using above)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
